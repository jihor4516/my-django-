# دليل الـ Backend بالعربية

## 1. نظرة عامة

الـ backend مبني بـ Django 4.2، ومهمته الأساسية هي:

- إدارة الحسابات والمصادقة والتحقق بالبريد.
- إدارة العملاء والمستأجرين.
- إدارة مخزون المعدات والتصنيفات والصور.
- معالجة طلبات الإيجار وتحويلها إلى عقود.
- توليد العقود والمستندات بصيغ HTML و PDF.
- تزويد الـ frontend بواجهات API بصيغة JSON.

المجلد الأساسي للـ backend هو:

`backend/`

## 2. كيف يتحرك الطلب داخل الـ backend

عند وصول طلب من الواجهة الأمامية، يمر عادة بهذه السلسلة:

1. يدخل من خلال `config/urls.py` أو من خلال `apps/api/urls.py` إذا كان طلب API.
2. يتم توجيهه إلى View مناسبة داخل التطبيق الفرعي المسؤول.
3. تقوم الـ View بقراءة أو تحديث البيانات من Models.
4. إذا كان الطلب API ترجع النتيجة JSON.
5. إذا كان الطلب خاصًا بالعقود أو المستندات، يتم تجهيز Context ثم استخدام قالب HTML أو تحويله إلى PDF.

## 3. الملفات الجذرية في backend

### `backend/manage.py`
- نقطة تشغيل أوامر Django.
- يستخدم لتشغيل السيرفر، الهجرات، الفحص، وإنشاء المستخدمين الإداريين.

### `backend/.env.example`
- ملف مثال لإعدادات البيئة المحلية.
- الهدف منه توضيح المتغيرات التي يمكن تفعيلها بدون تخزين أسرار حقيقية داخل المستودع.

### `backend/.gitignore`
- يمنع إدخال ملفات محلية حساسة مثل `.env` إلى Git.

### `backend/db.sqlite3`
- قاعدة البيانات الحالية للمشروع في بيئة التطوير.
- مناسبة للتجارب والتسليم المحلي السريع، وليست الخيار الأفضل للإنتاج الكبير.

### `backend/locale/`
- يحتوي ملفات الترجمة الخاصة بـ Django للغات المدعومة.

### `backend/media/`
- يحتوي الملفات المرفوعة من المستخدمين أو الإدارة مثل الصور والوثائق والتواقيع.

### `backend/static/`
- يحتوي الأصول الثابتة مثل الصور العامة والشعارات وملفات CSS/JS الثابتة الخاصة بـ Django.

## 4. مجلد `backend/config/`

### `backend/config/__init__.py`
- يعرّف المجلد كحزمة Python.

### `backend/config/settings.py`
- أهم ملف إعدادات في المشروع الحالي.
- يحدد التطبيقات المثبتة `INSTALLED_APPS`.
- يضبط Middleware ومن ضمنها Middleware بسيط للـ CORS.
- يحدد قاعدة البيانات، اللغات، ملفات static/media.
- يحمّل متغيرات البيئة من `.env` تلقائيًا.
- يضبط إعدادات البريد عبر SMTP من متغيرات البيئة.

### `backend/config/urls.py`
- نقطة التجميع الرئيسية لكل مسارات Django.
- يربط:
  - مسارات API تحت `/api/`
  - لوحة الإدارة Django تحت `/gtr-admin/`
  - مسارات dashboard والعقود والمستندات والحسابات
  - إعادة توجيه الصفحة العامة إلى واجهة Next.js

### `backend/config/asgi.py`
- ملف تشغيل ASGI عند استخدام خادم يدعم async.

### `backend/config/wsgi.py`
- ملف تشغيل WSGI التقليدي لخوادم Django القياسية.

## 5. معنى الملفات المشتركة المتكررة في كل تطبيق Django

هذه الملفات تتكرر غالبًا في أغلب التطبيقات الفرعية:

### `__init__.py`
- يعرّف المجلد كحزمة Python.

### `apps.py`
- تعريف إعدادات التطبيق داخل Django مثل الاسم المعتمد في `INSTALLED_APPS`.

### `admin.py`
- تسجيل النماذج داخل لوحة إدارة Django إذا كانت مستخدمة هناك.

### `models.py`
- تعريف الجداول الرئيسية وعلاقات البيانات.

### `views.py`
- منطق الطلبات والاستجابات.

### `urls.py`
- ربط المسارات الخاصة بذلك التطبيق بالـ Views المناسبة.

### `tests.py`
- مكان الاختبارات الآلية الخاصة بالتطبيق.

### `migrations/`
- ملفات الهجرات الخاصة ببنية قاعدة البيانات.

## 6. التطبيقات الفرعية داخل `backend/apps/`

### 6.1 تطبيق `accounts`

مسؤول عن:

- تسجيل الحسابات.
- تسجيل الدخول والخروج.
- الملف الشخصي للمستخدم.
- أدوار الموظفين.
- أكواد التحقق عبر البريد.

#### `backend/apps/accounts/models.py`
- يحتوي النماذج التالية:
  - `UserProfile`: بيانات المستخدم الإضافية مثل النوع، الهاتف، العنوان، المدينة، الوثائق، اللغة.
  - `StaffProfile`: بيانات الموظف الداخلي والدور الإداري.
  - `LoginLog`: سجل محاولات الدخول.
  - `VerificationCode`: أكواد تفعيل الحساب المؤقتة.

#### `backend/apps/accounts/forms.py`
- يحتوي نماذج Django Forms الخاصة بالتسجيل والدخول.
- مهم جدًا لأن التحقق الخلفي يعتمد عليه، ومنه ظهر سابقًا أن `address` حقل مطلوب.

#### `backend/apps/accounts/views.py`
- يحتوي Views تقليدية قديمة تعتمد إعادة التوجيه إلى الواجهة الأمامية.
- دوره الآن جسر بين بعض مسارات Django القديمة وواجهة Next.js.
- يضم أيضًا إعادة إرسال كود التحقق في النمط التقليدي.

#### `backend/apps/accounts/urls.py`
- يربط مسارات مثل:
  - التسجيل
  - الدخول
  - الخروج
  - التحقق
  - الملف الشخصي
  - لوحة العميل

#### `backend/apps/accounts/permissions.py`
- يحتوي فحوصات الصلاحيات ودوال التحقق من كون المستخدم موظفًا أو صاحب صلاحية إدارية.
- يستخدم على نطاق واسع داخل dashboard وAPI.

#### `backend/apps/accounts/email_utils.py`
- ملف مساعد أضيف لتوحيد إرسال بريد التحقق.
- يحدد هل إعدادات البريد الحقيقية مفعلة أم لا.
- يرسل الرسالة أو يسمح بردّ تطويري صادق عندما لا تكون SMTP مضبوطة.

#### `backend/apps/accounts/admin.py`
- يربط نماذج الحسابات مع لوحة إدارة Django.

#### `backend/apps/accounts/tests.py`
- مخصص لاختبارات الحسابات.

### 6.2 تطبيق `api`

هذا هو قلب الاتصال بين Django وNext.js.

#### `backend/apps/api/urls.py`
- يعرّف جميع مسارات API بصيغة JSON مثل:
  - الصحة العامة `health`
  - الأصناف والمعدات
  - تسجيل الدخول والتسجيل والتفعيل
  - لوحة العميل
  - لوحة الموظف
  - الإشعارات
  - المستندات
  - طلبات الإيجار
  - إدارة الأصناف والمعدات للموظف

#### `backend/apps/api/views.py`
- أهم ملف منطق API في المشروع.
- يحتوي دوال مساعدة داخلية لبناء Payloads موحدة مثل:
  - بيانات الصنف
  - بيانات المعدة
  - بيانات العقد
  - بيانات الإشعار
  - بيانات العميل
  - بيانات المستأجر
- ينفذ عمليات مثل:
  - `auth_signup`
  - `auth_login`
  - `auth_verify`
  - `tenant_dashboard`
  - `staff_dashboard`
  - إنشاء طلبات الإيجار
  - معالجة الطلبات من جهة الإدارة
  - إنشاء مستندات إدارية
  - إنشاء تصنيفات ومعدات وصور جديدة

#### `backend/apps/api/middleware.py`
- Middleware بسيط لإتاحة الاتصال بين الواجهة الأمامية والـ backend محليًا.

#### `backend/apps/api/apps.py`
- تعريف التطبيق داخل Django.

### 6.3 تطبيق `contracts`

مسؤول عن طلبات الإيجار والعقود وملفات الإرجاع والطباعة.

#### `backend/apps/contracts/models.py`
- يحتوي النماذج التالية:
  - `RentalContract`: العقد الرئيسي.
  - `ContractItem`: تفاصيل المعدات داخل العقد.
  - `ContractHistory`: سجل تغيرات العقد.
  - `RentalRequest`: طلب الإيجار قبل الموافقة.

#### `backend/apps/contracts/views.py`
- يولد معاينة العقد.
- يولد PDF للعقد.
- يولد معاينة ووثيقة الإرجاع.
- يجهز `context` للقوالب مع الشعار والبيانات القانونية.
- يحتوي حماية وصول حتى لا يفتح العقد إلا صاحبه أو الموظف المخول.

#### `backend/apps/contracts/urls.py`
- يربط روابط:
  - معاينة العقد
  - PDF العقد
  - معاينة وثيقة الإرجاع
  - PDF وثيقة الإرجاع
  - إنشاء طلب إيجار تقليدي
  - التحقق من مرجع العقد

#### `backend/apps/contracts/pdf.py`
- ملف مساعد لتحويل قوالب HTML إلى PDF.
- يعتمد عليه كل من العقود والمستندات الإدارية.

#### `backend/apps/contracts/templates/pdf/contract.html`
- قالب HTML المستخدم لمعاينة العقد وطباعته.
- الشعار أصبح صورة حقيقية.
- QR Code أزيل منه نهائيًا في النسخة الحالية.

#### `backend/apps/contracts/templates/pdf/restitution_fr.html`
- قالب وثيقة الإرجاع الخاصة بالعقد.

#### `backend/apps/contracts/admin.py`
- تسجيل نماذج العقود داخل لوحة Django.

#### `backend/apps/contracts/tests.py`
- موضع اختبارات منطق العقود.

### 6.4 تطبيق `dashboard`

وظيفته الأصلية كانت توفير لوحة Django داخلية، لكنه اليوم يعمل أساسًا كطبقة ربط وصلاحيات مع واجهة الإدارة في Next.js.

#### `backend/apps/dashboard/models.py`
- يحتوي `SystemSettings`.
- هذا النموذج يخزن إعدادات المؤسسة العامة مثل:
  - اللغة الافتراضية
  - نسبة TVA
  - البيانات القانونية للمسرح
  - البريد القانوني والنص السفلي

#### `backend/apps/dashboard/views.py`
- يعالج بعض العمليات الإدارية القديمة.
- يحتوي منطقًا مهمًا في معالجة طلب الإيجار التقليدية وتحويلها إلى عقد.
- كثير من الـ Views هنا تعيد التوجيه إلى صفحات Next.js بدل عرض قوالب Django.

#### `backend/apps/dashboard/urls.py`
- يربط روابط لوحة التحكم والإعدادات وطلبات الإيجار.

#### `backend/apps/dashboard/context_processors.py`
- يحقن إعدادات النظام داخل قوالب Django عند الحاجة.

#### `backend/apps/dashboard/forms.py`
- النماذج المتعلقة بالإعدادات أو مدخلات اللوحة التقليدية.

#### `backend/apps/dashboard/admin.py`
- تسجيل إعدادات النظام داخل لوحة Django.

#### `backend/apps/dashboard/tests.py`
- اختبارات لوحة الإدارة والإعدادات.

### 6.5 تطبيق `documents`

مسؤول عن الوثائق الإدارية غير المرتبطة بالعقد مباشرة، مثل السندات والوثائق التشغيلية.

#### `backend/apps/documents/models.py`
- يحتوي:
  - `ExternalDocument`: المستند الرئيسي.
  - `DocumentItem`: صفوف المعدات داخل المستند.

#### `backend/apps/documents/views.py`
- ينشئ مستندًا جديدًا.
- يعرض المستند كمعاينة HTML.
- يصدّر المستند PDF.
- يجهز محتوى الصفوف والشعار وعنوان الوثيقة حسب النوع.

#### `backend/apps/documents/urls.py`
- يربط:
  - قائمة المستندات
  - إنشاء مستند
  - التفاصيل
  - المعاينة
  - PDF

#### `backend/apps/documents/templates/pdf/document.html`
- القالب العام للمستندات.

#### `backend/apps/documents/templates/pdf/decharge.html`
- قالب خاص بنوع décharge.

#### `backend/apps/documents/templates/pdf/reception.html`
- قالب خاص بوثيقة الاستلام.

#### `backend/apps/documents/templates/pdf/commande.html`
- قالب خاص بأمر/سند الطلب.

#### `backend/apps/documents/templates/pdf/pv.html`
- قالب خاص بالمحضر أو procès-verbal.

#### `backend/apps/documents/admin.py`
- تسجيل النماذج في لوحة الإدارة.

#### `backend/apps/documents/tests.py`
- اختبارات المستندات.

### 6.6 تطبيق `inventory`

هذا هو مخزن المعدات والتصنيفات والصور.

#### `backend/apps/inventory/models.py`
- يحتوي:
  - `Category`: تصنيف المعدات متعدد اللغات.
  - `Item`: المعدة نفسها وكل بياناتها التجارية واللوجستيكية.
  - `ItemImage`: صور المعدة.
  - `MaintenanceLog`: سجلات الصيانة.

#### `backend/apps/inventory/views.py`
- منطق العرض أو العمليات التقليدية للمخزون خارج الـ API.

#### `backend/apps/inventory/urls.py`
- مسارات المخزون والتفاصيل في النمط التقليدي.

#### `backend/apps/inventory/templatetags/__init__.py`
- تعريف حزمة فلاتر القوالب.

#### `backend/apps/inventory/templatetags/inventory_extras.py`
- فلاتر أو Tags مخصصة لقوالب Django الخاصة بالمخزون.

#### `backend/apps/inventory/admin.py`
- تسجيل نماذج المخزون في لوحة Django.

#### `backend/apps/inventory/tests.py`
- اختبارات المخزون.

### 6.7 تطبيق `notifications`

#### `backend/apps/notifications/models.py`
- يحتوي نموذج `Notification`.
- يمثل الإشعار الداخلي الذي يرسله الموظف إلى عميل أو عدة عملاء.
- يخزن العنوان، النص، الرابط، حالة القراءة، والمرسل.

#### `backend/apps/notifications/views.py`
- منطق إضافي إن وُجد لإدارة الإشعارات خارج طبقة API.

#### `backend/apps/notifications/admin.py`
- تسجيل نموذج الإشعارات داخل لوحة الإدارة.

#### `backend/apps/notifications/tests.py`
- اختبارات الإشعارات.

### 6.8 تطبيق `reports`

#### `backend/apps/reports/models.py`
- حاليًا شبه فارغ ويبدو كمكان محجوز لتطوير التقارير لاحقًا.

#### `backend/apps/reports/views.py`
- مخصص مستقبلًا لمنطق التقارير أو التصدير.

#### `backend/apps/reports/admin.py`
- إعدادات لوحة الإدارة الخاصة بالتقارير.

#### `backend/apps/reports/tests.py`
- اختبارات التقارير.

### 6.9 تطبيق `tenants`

يمثل الكيان التجاري أو القانوني الذي يستأجر من المسرح.

#### `backend/apps/tenants/models.py`
- يحتوي نموذج `Tenant`.
- يخزن معلومات الجهة المستأجرة مثل:
  - النوع
  - الاسم الكامل
  - المرجع القانوني
  - الهاتف والبريد
  - العنوان والمدينة
  - اللغة المفضلة
  - التصنيف والحظر وعدد العقود وإجمالي الإنفاق

#### `backend/apps/tenants/views.py`
- أي Views مرتبطة بالمستأجرين خارج طبقة API.

#### `backend/apps/tenants/admin.py`
- تسجيل نموذج المستأجرين في لوحة Django.

#### `backend/apps/tenants/tests.py`
- اختبارات المستأجرين.

## 7. مسارات العمل الأساسية في الـ backend

### تسجيل حساب جديد
1. الـ frontend يرسل البيانات إلى `apps/api/views.py`.
2. يتم التحقق عبر `SignUpForm` في `accounts/forms.py`.
3. تُنشأ بيانات `User` و `UserProfile`.
4. يُولد `VerificationCode`.
5. يحاول `email_utils.py` إرسال الكود بالبريد.
6. عند التحقق يصبح الحساب صالحًا للدخول.

### تقديم طلب إيجار
1. العميل يفتح معدة من الكتالوج.
2. يرسل الطلب إلى API.
3. يتم إنشاء `RentalRequest` داخل `contracts/models.py`.
4. يظهر الطلب في لوحة الموظف.

### الموافقة على طلب إيجار
1. الموظف يراجع الطلب من لوحة الإدارة.
2. يتم فحص الكمية المتاحة.
3. يُنشأ أو يُربط `Tenant` بالمستخدم.
4. يُنشأ `RentalContract` و `ContractItem`.
5. تُحدّث كميات المخزون.
6. يصبح العقد متاحًا للمعاينة والطباعة والتنزيل.

### إنشاء مستند إداري
1. الموظف يحدد نوع الوثيقة والعميل والعناصر.
2. API ينشئ `ExternalDocument` و `DocumentItem`.
3. المستند يصبح جاهزًا للمعاينة وPDF.

## 8. ملاحظات مهمة لمطور يستلم المشروع

### البريد الإلكتروني
- إرسال البريد الحقيقي يعتمد على متغيرات SMTP في `backend/config/settings.py`.
- إذا لم تكن مضبوطة، قد يعمل النظام في نمط تطويري يعرض الكود بدل إرساله فعليًا.

### الترجمة
- Django يدعم الترجمة على مستوى backend والواجهة التقليدية.
- لكن الجزء الأكبر من النصوص المعروضة للمستخدم النهائي موجود في frontend locales.

### التعايش بين Django وNext.js
- المشروع هجين.
- توجد مسارات Django تقليدية قديمة، لكن تجربة الاستخدام الحديثة تعتمد أساسًا على Next.js + JSON API.

### ملف إعدادات إضافي غير فعّال حاليًا
- يوجد ملف: `grand_theatre_rental/settings.py` داخل الجذر الخارجي للمشروع.
- هذا يبدو كهيكل Django مولد قديم أو بديل غير المستخدم فعليًا في التشغيل الحالي.
- التشغيل الفعلي المعتمد الآن يعتمد على `backend/config/settings.py`.

## 9. خلاصة معمارية الـ backend

يمكن فهم الـ backend في هذا المشروع على أنه ثلاث طبقات رئيسية:

1. طبقة البيانات: موجودة في `models.py` داخل التطبيقات المختلفة.
2. طبقة الخدمة/المنطق: موجودة أساسًا في `apps/api/views.py` وبعض Views التطبيقات.
3. طبقة الإخراج: إما JSON للواجهة الأمامية أو HTML/PDF للعقود والمستندات.

إذا كان المطور الجديد يريد نقطة دخول واحدة لفهم النظام بسرعة، فابدأ بهذه الملفات بهذا الترتيب:

1. `backend/config/settings.py`
2. `backend/config/urls.py`
3. `backend/apps/api/urls.py`
4. `backend/apps/api/views.py`
5. `backend/apps/contracts/models.py`
6. `backend/apps/inventory/models.py`
7. `backend/apps/documents/views.py`
